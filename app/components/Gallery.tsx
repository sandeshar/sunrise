import SectionTitle from "./helper/SectionTitle";

const Gallery = () => {
    return (
        <div className="pt-20">
            <SectionTitle title="Gallery" />
        </div>
    );
};

export default Gallery;